version: '3.8'

services:
  yugioh-personalcollection:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yugioh-personalCollection
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1